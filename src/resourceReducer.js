import { createReducer } from "./utils/reducerUtils";
import resourceOpts from "./content/resources/resourceOpts";

const defaultState = {
    resourceOpts
}

export default createReducer( defaultState, {});
